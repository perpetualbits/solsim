//! On-screen user interface built with egui.
//!
//! For now this is the in-app [`manual`]; the status overlay (HUD) still lives in
//! `main.rs`. Keeping the UI text apart from the simulation keeps both readable.

pub mod manual;
